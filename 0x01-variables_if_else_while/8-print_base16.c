#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char d;

	int h;

	d = 'a'';
	while (h < 16)
	{
		putchar('h + '0');
		h++;
	}
	while (d <= 'f')
	{
		putchar(d);
		d++;
	}
	return (0);
}
